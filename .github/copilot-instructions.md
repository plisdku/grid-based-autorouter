# Copilot / AI agent instructions — grid-based-autorouter

Overview
- This repository is a Rust/Cargo project (see `./.gitignore`), so assume Cargo-based builds, tests, and formatting.
- Be succinct. Only produce code changes when explicitly requested by a human reviewer.

